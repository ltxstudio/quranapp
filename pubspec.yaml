name: alquranbd
description: Al-Quran with Bengali translation.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  assets_audio_player: ^3.0.4
  flutter_lints: ^1.0.4
  flutter_native_splash: ^2.1.6
  flutter_typeahead: ^3.2.5
  font_awesome_flutter: ^10.1.0
  google_fonts: ^2.3.2
  http: ^0.13.4
  url_launcher: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/quran_bn.json
    - assets/audio/
